<%= form_tag action, {class:'navbar-form', target: :_blank} do %>
    <div class="form-group">
      <%= text_field_tag :keywords, '', {class: 'form-control', placeholder:t('placeholders.search')} %>
    </div>
    <%= submit_tag t('buttons.search'), {class: 'btn btn-success'} %>
<% end %>