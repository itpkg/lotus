<h3>{{t 'auth.users.didn_t_receive_unlock_instructions'}}</h3>
<hr/>
<form {{action "unlock" on="submit"}}>
  <div class="form-group">
    <label>{{t 'auth.attributes.user.email'}}</label>
    {{input type="email" value=email class="form-control"}}
  </div>
  <button type="submit" class="btn btn-primary">{{t 'auth.users.resend_unlock_instructions'}}</button>
</form>
